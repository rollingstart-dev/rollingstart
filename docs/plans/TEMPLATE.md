# M{N}: {Title}

> Expanded plan for the `M{N}: {Title}` milestone. The roadmap entry is
> [`docs/roadmap.md`](../roadmap.md) § M{N}.
>
> This file is the drafting artifact. Its sub-scopes are copied verbatim into
> GitHub issues; its remaining content becomes the project board's README. After
> the milestone ships, `/milestone-endgame` appends a retrospective.

## Context

What the previous milestone delivered. What this one delivers. The end state —
what is true when this milestone is done that isn't true now.

## Key decisions

| Decision | Choice | Why |
|---|---|---|

Anything meeting the ADR threshold gets a record in
[`docs/decisions/`](../decisions/) instead of a table row here.

## Sub-scopes

### {N}.1 — {Title} [PENDING]

**Goal.** One sentence.

**Branch.** `m{N}.1/{slug}`

**Depends on.** Nothing, or `{N}.x`.

**Acceptance criteria.**

- Stated as outcomes, not file lists — the implementor chooses the files
- Testable: someone else can confirm each one independently

**Verification.**

- [ ] What must be observed for this sub-scope to be done

---

### {N}.2 — {Title} [PENDING]

...

## Explicitly deferred

What is out of scope for this milestone, and why. Naming it here stops it being
re-litigated mid-implementation.

## Verification

End-to-end criteria for the milestone as a whole, distinct from any single
sub-scope's. The closure sub-scope confirms these.
